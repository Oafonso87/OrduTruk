import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ordutruk.app',
  appName: 'OrduTruk',
  webDir: 'dist/ordutruk/browser'
};

export default config;
